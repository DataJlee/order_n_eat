server:
  port: 5000

spring:
  datasource:
    url: jdbc:mysql://oe-db-dev.c8tebabk8sof.ap-northeast-2.rds.amazonaws.com:3306/oedb?serverTimezone=Asia/Seoul
    username: admin
    password: Heehee2001!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: update
  config:
    activate:
      on-profile: dev

